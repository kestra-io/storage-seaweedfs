kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: seaweedfs
    seaweedfs:
      filer-host: localhost
      filer-port: 18888
      prefix: "kestra-test/"
      replication: "000"
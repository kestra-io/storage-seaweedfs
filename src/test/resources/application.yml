kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: seaweedfs
    seaweedfs:
      filer-host: ${seaweedfs.filer.host:localhost}
      filer-port: ${seaweedfs.filer.port:18888}
      prefix: ""
      replication: "000"